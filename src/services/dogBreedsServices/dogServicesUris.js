const server = "https://dog.ceo"
const context = "/api"
const breedUri = "/breed"
const breedsUri = "/breeds"
const list = "/list"
const all = "/all"
const images = "/images"

export {
    server,
    context,
    breedUri,
    breedsUri,
    list,
    all,
    images
}